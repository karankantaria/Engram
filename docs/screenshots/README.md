# Screenshots

Drop PNGs here with these names — the main `README.md` already references them:

| File | Suggested shot |
|------|----------------|
| `graph.png`   | The brain graph with a few clusters (the hero shot) |
| `capture.png` | The capture bar + a note open in the reader |
| `ask.png`     | The ⏎ ask panel showing a grounded answer with sources |
| `clusters.png`| A cluster focused / the cluster summary detail |

Tip: run the app, press the maximize button, and use Win+Shift+S (or a tool like
ShareX) to grab a clean region. ~1600px wide looks good on GitHub.
